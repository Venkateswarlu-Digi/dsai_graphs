# Base URL is optional only when your API is served from the same origin under /api.
VITE_API_BASE_URL=http://localhost:9090/api/v1/pdp
# VITE_API_TOKEN=replace-with-your-token

# Change these only if your backend uses different routes.
VITE_DEMAND_FORECAST_ENDPOINT=/demand_forecast
VITE_ANOMALY_DETECTOR_ENDPOINT=/anomaly_detector
VITE_SAFETY_STOCK_ENDPOINT=/safety_stock
VITE_SAP_PR_PO_ENDPOINT=/sap_pr_po
VITE_STOCKOUT_RISK_ENDPOINT=/stockout_risk
